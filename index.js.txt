const express = require('express');
const app = express();
const port = 3000;

app.get('/', (req, res) => {
  res.send('Hello! This is my first dApp with FHEVM.');
});

app.listen(port, () => {
  console.log(`App is running: http://localhost:${port}`);
});
